spring:
  application:
    name: '@ext.longName@'

logging:
  level:
    org:
      springframework: INFO

secure-aws-config:
  awsProfile: tim-dev
  parameters:
    pathBase: /sampleapi/pdev
    region: ap-southeast-2

