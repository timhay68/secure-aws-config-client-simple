spring:
  application:
    name: '@ext.longName@'
  profiles:
    active: awsParameterConfig,awsSecretConfig

logging:
  level:
    org:
      springframework: INFO

secure-aws-config:
  awsProfile: tim-dev
  parameters:
    pathBase: /sampleapi/dev
    region: ap-southeast-2
  secrets:
    secretName: sampleapi/dev
    region: ap-southeast-2

